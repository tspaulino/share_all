<% semantic_form_for [@current_user, @object, @comment] do |f| %>
  <%= f.error_messages %>       
  <% f.inputs @comment do %>
    <p>
      <%= f.input :text, :required => true %>
    </p>
  <% end %>
  <% f.buttons do %>  
    <p><%= f.commit_button %></p>
  <% end %>
<% end %>

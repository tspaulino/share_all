<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Strict//EN"
  "http://www.w3.org/TR/xhtml1/DTD/xhtml1-strict.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8">
    <title><%= h(yield(:title) || "Untitled") %></title>
    <%= stylesheet_link_tag 'application' %>
    <%= yield(:head) %>
    <%= formtastic_stylesheet_link_tag %>
    <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/swfobject/2.2/swfobject.js">
    </script>
    <%= javascript_include_tag "jwplayer.js" %>
    <%= javascript_include_tag :defaults %>
        
  </head>
  <body>  
    <div id="container">       
        <div id="top-nav">
          <% if @current_user %>
            Hello, <%= link_to @current_user.name, user_url(@current_user) %>!    |    <%= link_to "Logout", logout_url %>                     
          <% else %>
            <%= link_to "Login", login_url %>   or   <%= link_to "Signup", signup_url%>
          <% end %>
        </div>
      <%- flash.each do |name, msg| -%>
        <%= content_tag :div, msg, :id => "flash_#{name}" %>
      <%- end -%>

      <%- if show_title? -%>
        <h1><%=h yield(:title) %></h1>
      <%- end -%>      
      <%= yield %>
    </div>
  </body>
</html>
